import 'package:flutter/material.dart';

class SNoInternetConnection extends StatefulWidget {
  const SNoInternetConnection({super.key});

  @override
  State<SNoInternetConnection> createState() => _SNoInternetConnectionState();
}

class _SNoInternetConnectionState extends State<SNoInternetConnection> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
